import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sample.geolocation.app',
  appName: 'sample-capacitor-app-geolocation',
  webDir: 'dist'
};

export default config;
